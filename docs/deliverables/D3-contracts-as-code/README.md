# D3 - Contracts as Code

## Scope
- Add baseline alert rules in `alerts/`.
- Add dashboard + datasource provisioning in `dashboards/`.

## Expected Outcome
- Alerts include required labels.
- Provisioning loads without manual steps.

## Gate
- Alert lint/validation passes.
- Dashboard provisioning validation passes.
