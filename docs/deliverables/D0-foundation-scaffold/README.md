# D0 - Foundation Scaffold

## Scope
- Create top-level directories: `module/`, `platform/`, `examples/`, `alerts/`, `dashboards/`, `scripts/`.
- Add root `Makefile` commands: `up`, `down`, `test`, `lint`, `smoke` (stubs allowed).

## Expected Outcome
- Repository structure matches contract in docs.
- Make targets are discoverable and runnable.

## Gate
- Directory tree present and reviewed.
- Makefile reviewed and accepted.
